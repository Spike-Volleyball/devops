services:
    postgres:
        image: postgres:15-alpine
        container_name: volleyer-postgres
        environment:
            POSTGRES_USER: volleyer_user
            POSTGRES_PASSWORD: volleyer_password_dev
        ports:
            - "5432:5432"
        volumes:
            - postgres_data:/var/lib/postgresql/data
            - ./init:/docker-entrypoint-initdb.d
        networks:
            - volleyer_network
        restart: unless-stopped
        deploy:
            resources:
                limits:
                    cpus: '2'
                    memory: 2G
                reservations:
                    cpus: '0.5'
                    memory: 512M
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U volleyer_user"]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 40s
        command: postgres -c wal_level=logical -c max_replication_slots=4 -c max_wal_senders=4

    redis:
        image: redis:7-alpine
        container_name: volleyer-redis
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data
        networks:
            - volleyer_network
        restart: unless-stopped
        deploy:
            resources:
                limits:
                    cpus: '0.5'
                    memory: 256M
                reservations:
                    cpus: '0.1'
                    memory: 64M
        healthcheck:
            test: ["CMD", "redis-cli", "ping"]
            interval: 10s
            timeout: 5s
            retries: 3
        command: redis-server --appendonly yes

    rabbitmq:
        image: rabbitmq:3-management
        container_name: volleyer-rabbitmq
        ports:
            - "5672:5672" # AMQP
            - "15672:15672" # Management UI
        environment:
            RABBITMQ_DEFAULT_USER: volleyer_user
            RABBITMQ_DEFAULT_PASS: volleyer_password_dev
        volumes:
            - rabbitmq_data:/var/lib/rabbitmq
        networks:
            - volleyer_network
        restart: unless-stopped
        deploy:
            resources:
                limits:
                    cpus: '1'
                    memory: 512M
                reservations:
                    cpus: '0.25'
                    memory: 128M
        healthcheck:
            test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 30s

    mailpit:
        image: axllent/mailpit:v1.29.4
        container_name: volleyer-mailpit
        environment:
            MP_MAX_MESSAGES: 500
            MP_MAX_AGE: 24h
        ports:
            - "8025:8025"
            - "1025:1025"
        networks:
            - volleyer_network
        restart: unless-stopped
        healthcheck:
            test: ["CMD", "/mailpit", "readyz"]
            interval: 15s
            timeout: 5s
            retries: 3
            start_period: 10s

    auth-service:
        build:
            context: ../../microservices/auth-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-auth-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # ASPNETCORE_URLS intentionally unset so Kestrel binds the endpoints from
            # appsettings.json (HTTP on 5005, gRPC HTTP/2 on 5006).
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            - EmailSettings__SmtpTransportEnabled=true
            - EmailSettings__SmtpHost=volleyer-mailpit
            - EmailSettings__SmtpPort=1025
            - EmailSettings__SmtpUseTls=false
            - Urls__Web=https://dev.volleyspike.app
        ports:
            - "5005:5005" # HTTP
            - "5006:5006" # gRPC (HTTP/2)
            - "10000:10000" # Debugger
        volumes:
            - ../../microservices/auth-service:/app
            # Anonymous volume overlays: hide host bin/obj so MSBuild doesn't
            # pick up stale artifacts alongside container builds in /tmp/.
            # Without these, NETSDK1022 "duplicate Content items" errors occur.
            - /app/Auth/bin
            - /app/Auth/obj
            - /app/Auth.Domain/bin
            - /app/Auth.Domain/obj
            - /app/Auth.Infrastructure/bin
            - /app/Auth.Infrastructure/obj
            - /app/Auth.Services/bin
            - /app/Auth.Services/obj
            - /app/Auth.Tests/bin
            - /app/Auth.Tests/obj
            - /app/Auth.Tests.Integration/bin
            - /app/Auth.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            mailpit:
                condition: service_healthy

    events-service:
        build:
            context: ../../microservices/events-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-events-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Local only, as for clubs: the admin console's republish of fact history for rewards.
            - AdminConsole__ApiKey=${ADMIN_CONSOLE_API_KEY:-local-dev-only-not-a-secret}
        ports:
            - "5010:5010" # HTTP
            - "5011:5011" # gRPC
            - "10001:10001" # Debugger
        volumes:
            - ../../microservices/events-service:/app
            - /app/Events/bin
            - /app/Events/obj
            - /app/Events.Application/bin
            - /app/Events.Application/obj
            - /app/Events.Domain/bin
            - /app/Events.Domain/obj
            - /app/Events.Infrastructure/bin
            - /app/Events.Infrastructure/obj
            - /app/Events.Tests/bin
            - /app/Events.Tests/obj
            - /app/Events.Tests.Integration/bin
            - /app/Events.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy

    profiles-service:
        build:
            context: ../../microservices/profiles-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-profiles-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - ASPNETCORE_URLS=http://+:5170;http://+:5171
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
        ports:
            - "5170:5170" # HTTP
            - "5171:5171" # gRPC
            - "10002:10002" # Debugger
        volumes:
            - ../../microservices/profiles-service:/app
            # Anonymous volume overlays: hide host bin/obj so MSBuild doesn't
            # pick up stale artifacts alongside container builds in /tmp/.
            - /app/Profiles/bin
            - /app/Profiles/obj
            - /app/Profiles.Application/bin
            - /app/Profiles.Application/obj
            - /app/Profiles.Domain/bin
            - /app/Profiles.Domain/obj
            - /app/Profiles.Infrastructure/bin
            - /app/Profiles.Infrastructure/obj
            - /app/Profiles.Tests.Unit/bin
            - /app/Profiles.Tests.Unit/obj
            - /app/Profiles.Tests.Integration/bin
            - /app/Profiles.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy

    clubs-service:
        build:
            context: ../../microservices/clubs-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-clubs-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Local only. The admin endpoints are how an e2e test verifies a club, and
            # every messaging rule needs one. nginx.Development.conf has no admin deny
            # rule and the gateway is bound to localhost; the staging and production
            # compose files must NOT get this.
            - AdminConsole__ApiKey=${ADMIN_CONSOLE_API_KEY:-local-dev-only-not-a-secret}
        ports:
            - "5021:5021" # gRPC
            - "5020:5020" # HTTP
            - "10005:10005" # Debugger
        volumes:
            - ../../microservices/clubs-service:/app
            - /app/Clubs/bin
            - /app/Clubs/obj
            - /app/Clubs.Application/bin
            - /app/Clubs.Application/obj
            - /app/Clubs.Domain/bin
            - /app/Clubs.Domain/obj
            - /app/Clubs.Infrastructure/bin
            - /app/Clubs.Infrastructure/obj
            - /app/Clubs.Tests.Unit/bin
            - /app/Clubs.Tests.Unit/obj
            - /app/Clubs.Tests.Integration/bin
            - /app/Clubs.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy

    messages-service:
        build:
            context: ../../microservices/messages-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-messages-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - ASPNETCORE_URLS=http://+:5180
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__1=https://dev.volleyspike.app
        ports:
            - "5180:5180" # HTTP
            - "10003:10003" # Debugger
        volumes:
            - ../../microservices/messages-service:/app
            - /app/Messages/bin
            - /app/Messages/obj
            - /app/Messages.Application/bin
            - /app/Messages.Application/obj
            - /app/Messages.Domain/bin
            - /app/Messages.Domain/obj
            - /app/Messages.Infrastructure/bin
            - /app/Messages.Infrastructure/obj
            - /app/Messages.SocketServer/bin
            - /app/Messages.SocketServer/obj
            - /app/Messages.Tests/bin
            - /app/Messages.Tests/obj
            - /app/Messages.Tests.Integration/bin
            - /app/Messages.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy

    notifications-service:
        build:
            context: ../../microservices/notifications-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-notifications-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - ASPNETCORE_URLS=http://+:5030;http://+:5031
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            - EmailSettings__SmtpTransportEnabled=true
            - EmailSettings__SmtpHost=volleyer-mailpit
            - EmailSettings__SmtpPort=1025
            - EmailSettings__SmtpUseTls=false
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Every emailed link this service builds — family and club invitations — has to be
            # a host a phone can open. appsettings.Development.json says localhost:3000, which
            # only the workstation's own browser can reach. Same override auth-service carries.
            - Urls__Web=https://dev.volleyspike.app
        ports:
            - "5030:5030" # HTTP
            - "5031:5031" # gRPC
            - "10006:10006" # Debugger
        volumes:
            - ../../microservices/notifications-service:/app
            - /app/Notifications/bin
            - /app/Notifications/obj
            - /app/Notifications.Application/bin
            - /app/Notifications.Application/obj
            - /app/Notifications.Domain/bin
            - /app/Notifications.Domain/obj
            - /app/Notifications.Infrastructure/bin
            - /app/Notifications.Infrastructure/obj
            - /app/Notifications.Tests.Unit/bin
            - /app/Notifications.Tests.Unit/obj
            - /app/Notifications.Tests.Integration/bin
            - /app/Notifications.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy
            redis:
                condition: service_healthy

    social-service:
        build:
            context: ../../microservices/social-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-social-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - ASPNETCORE_URLS=http://+:5040
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Local only, as for clubs: the admin console's republish of fact history for rewards.
            - AdminConsole__ApiKey=${ADMIN_CONSOLE_API_KEY:-local-dev-only-not-a-secret}
        ports:
            - "5040:5040" # HTTP
            - "10007:10007" # Debugger
        volumes:
            - ../../microservices/social-service:/app
            - /app/Social/bin
            - /app/Social/obj
            - /app/Social.Application/bin
            - /app/Social.Application/obj
            - /app/Social.Domain/bin
            - /app/Social.Domain/obj
            - /app/Social.Infrastructure/bin
            - /app/Social.Infrastructure/obj
            - /app/Social.Tests.Unit/bin
            - /app/Social.Tests.Unit/obj
            - /app/Social.Tests.Integration/bin
            - /app/Social.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy
            redis:
                condition: service_healthy

    rewards-service:
        build:
            context: ../../microservices/rewards-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-rewards-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - ASPNETCORE_URLS=http://+:5070
            - DOTNET_RUNNING_IN_CONTAINER=true
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Local only, as for clubs: the admin console's republish of fact history for rewards.
            - AdminConsole__ApiKey=${ADMIN_CONSOLE_API_KEY:-local-dev-only-not-a-secret}
        ports:
            - "5070:5070" # HTTP
            - "10010:10010" # Debugger
        volumes:
            - ../../microservices/rewards-service:/app
            - /app/Rewards/bin
            - /app/Rewards/obj
            - /app/Rewards.Application/bin
            - /app/Rewards.Application/obj
            - /app/Rewards.Domain/bin
            - /app/Rewards.Domain/obj
            - /app/Rewards.Infrastructure/bin
            - /app/Rewards.Infrastructure/obj
            - /app/Rewards.Tests.Unit/bin
            - /app/Rewards.Tests.Unit/obj
            - /app/Rewards.Tests.Integration/bin
            - /app/Rewards.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy
            redis:
                condition: service_healthy

    coaching-service:
        build:
            context: ../../microservices/coaching-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-coaching-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
            # Local only, as for clubs: the admin console's republish of fact history for rewards.
            - AdminConsole__ApiKey=${ADMIN_CONSOLE_API_KEY:-local-dev-only-not-a-secret}
        ports:
            - "5060:5060" # HTTP
            - "5061:5061" # gRPC
            - "10009:10009" # Debugger
        volumes:
            - ../../microservices/coaching-service:/app
            - /app/Coaching/bin
            - /app/Coaching/obj
            - /app/Coaching.Application/bin
            - /app/Coaching.Application/obj
            - /app/Coaching.Domain/bin
            - /app/Coaching.Domain/obj
            - /app/Coaching.Infrastructure/bin
            - /app/Coaching.Infrastructure/obj
            - /app/Coaching.Tests.Unit/bin
            - /app/Coaching.Tests.Unit/obj
            - /app/Coaching.Tests.Integration/bin
            - /app/Coaching.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy

    payments-service:
        build:
            context: ../../microservices/payments-service
            dockerfile: Dockerfile.dev
        container_name: volleyer-payments-dev
        environment:
            - ASPNETCORE_ENVIRONMENT=Development
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
            - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
            - AWS_REGION=${AWS_REGION:-us-east-1}
            - DOTNET_RUNNING_IN_CONTAINER=true
            - DOTNET_USE_POLLING_FILE_WATCHER=false
            - MSBUILDDISABLENODEREUSE=1
            # The web app is also served from the Cloudflare tunnel, and a browser calling
            # dev-api from that origin is cross-origin — without this the request is blocked
            # and the page fails in a way that looks like a broken endpoint.
            - Cors__AllowedOrigins__2=https://dev.volleyspike.app
        ports:
            - "5051:5051" # gRPC
            - "5050:5050" # HTTP
            - "10008:10008" # Debugger
        volumes:
            - ../../microservices/payments-service:/app
            - /app/Payments/bin
            - /app/Payments/obj
            - /app/Payments.Application/bin
            - /app/Payments.Application/obj
            - /app/Payments.Domain/bin
            - /app/Payments.Domain/obj
            - /app/Payments.Infrastructure/bin
            - /app/Payments.Infrastructure/obj
            - /app/Payments.Tests.Unit/bin
            - /app/Payments.Tests.Unit/obj
            - /app/Payments.Tests.Integration/bin
            - /app/Payments.Tests.Integration/obj
            - /app/shared/Shared/bin
            - /app/shared/Shared/obj
            - /app/shared/Shared.Contracts/bin
            - /app/shared/Shared.Contracts/obj
            - /app/shared/Shared.DataAccess/bin
            - /app/shared/Shared.DataAccess/obj
            - /app/shared/Shared.Logging/bin
            - /app/shared/Shared.Logging/obj
            - /app/shared/Shared.Messaging/bin
            - /app/shared/Shared.Messaging/obj
            - /app/shared/Shared.Messaging.Contracts/bin
            - /app/shared/Shared.Messaging.Contracts/obj
            - /app/shared/Shared.Microservices/bin
            - /app/shared/Shared.Microservices/obj
            - /app/shared/Shared.Services/bin
            - /app/shared/Shared.Services/obj
            - /app/shared/Shared.Guardian/bin
            - /app/shared/Shared.Guardian/obj
            - /app/shared/Shared.Testing/bin
            - /app/shared/Shared.Testing/obj
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            postgres:
                condition: service_healthy
            rabbitmq:
                condition: service_healthy

    otel-collector:
        image: otel/opentelemetry-collector-contrib:0.147.0
        container_name: otel-collector
        command: ['--config=/etc/otelcol/config.yml']
        restart: unless-stopped
        volumes:
            - ./otel-collector-config.dev.yml:/etc/otelcol/config.yml:ro
        networks:
            - volleyer_network
        ports:
            - "127.0.0.1:4317:4317"
            - "127.0.0.1:4318:4318"
        deploy:
            resources:
                limits:
                    memory: 512M

    nginx:
        image: nginx:alpine
        container_name: volleyer-nginx
        ports:
            - "8000:8000"
        volumes:
            - ../nginx/nginx.Development.conf:/etc/nginx/nginx.conf:ro
            - nginx_logs:/var/log/nginx
        networks:
            - volleyer_network
        restart: unless-stopped
        depends_on:
            - postgres
            - auth-service
            - events-service
            - profiles-service
            - clubs-service
            - messages-service
            - notifications-service
            - social-service
            - payments-service
            - coaching-service
            - rewards-service

volumes:
    postgres_data:
        driver: local
    redis_data:
        driver: local
    rabbitmq_data:
        driver: local
    nginx_logs:
        driver: local

networks:
    volleyer_network:
        driver: bridge
